{
  "name": "audioguide",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Аудиогид музея «Музей друзей», Красная Поляна",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.server.json --noEmit",
    "lint": "oxlint src server",
    "test": "vitest run",
    "test:watch": "vitest",
    "server:dev": "node server/local.ts",
    "server:hash": "node server/hash-password.ts",
    "server:build": "tsc -p tsconfig.server.build.json && node -e \"require('fs').writeFileSync('dist-server/package.json', JSON.stringify({type:'module'}))\""
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/oswald": "^5.3.0",
    "@fontsource/noto-sans-arabic": "^5.3.0",
    "@preact/signals": "^2.11.1",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@testing-library/preact": "^3.2.4",
    "@types/node": "^26.2.0",
    "happy-dom": "^20.11.2",
    "oxlint": "^1.78.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
